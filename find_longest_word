myString = input("My string is: ")
list = str.split(myString)
longest_word = max(list, key=len)
pos = list.index(longest_word)

print("The longest word in the string is:", longest_word)
print("The position of the longest word in the list is:", pos)
